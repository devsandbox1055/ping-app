version: '3.8'

services:
  backend:
    build: .
    container_name: coupleapp-backend
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=False
    restart: unless-stopped